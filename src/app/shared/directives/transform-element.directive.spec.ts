import { TransformElementDirective } from './transform-element.directive';

describe('TransformElementDirective', () => {
  it('should create an instance', () => {
    const directive = new TransformElementDirective();
    expect(directive).toBeTruthy();
  });
});
